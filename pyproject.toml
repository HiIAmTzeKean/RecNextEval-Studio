[project]
name = "recnexteval-studio"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "recnexteval-studio-backend",
]

[tool.uv]
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'aarch64'",
    "sys_platform == 'darwin'",
]

[tool.uv.workspace]
members = [
    "recnexteval-studio-backend",
]

[tool.uv.sources]
recnexteval-studio-backend = { workspace = true }
